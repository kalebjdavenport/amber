import React from 'react';
import PrimaryHeader from '../PrimaryHeader';

export default {
  component: PrimaryHeader,
  title: 'PrimaryHeader',
  parameters: {
		layout: 'fullscreen'
	}
}

export const Default = () => <PrimaryHeader />